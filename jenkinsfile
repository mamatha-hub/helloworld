pipeline {

    agent 
    { 
            label 'jenkins_slave'
    }
     stages {
    
        stage('Build') {         
            steps {
                sh 'mvn clean package'
            }
        }
        }
        }
