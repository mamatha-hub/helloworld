pipeline {

    agent {
        
        docker {
             
            image 'maven:3-alpine'
            label 'jenkins_slave'
            args '-v /root/.m2:/root/.m2'
            
        }
    }
     stages {
        stage('Build') {         
            steps {
                sh 'mvn clean package'
            }
        }
        }
        }
