pipeline {
      agent 
    { 
            label 'jenkins_slave'
    }

  stages {
    stage('Build') {
      steps {
        container('mavenimage') {
          sh """ mvn package -DskipTests """
        }
      }
    }
    }
    }
